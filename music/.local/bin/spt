#!/bin/sh

SPOTIFYD_CMD="spotifyd"

if [[ -z $(ps aux | grep "$SPOTIFYD_CMD\$") ]]; then
  if [[ -z $BW_SESSION ]]; then
    export BW_SESSION=$(bw unlock --raw)
  fi

  $SPOTIFYD_CMD
fi

/usr/bin/spt
