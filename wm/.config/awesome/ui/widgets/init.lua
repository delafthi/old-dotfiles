return {
  button = require("ui.widgets.button"),
  container = require("ui.widgets.container"),
  menu = require("ui.widgets.menu"),
  --[[ playerctl = require("ui.widgets.playerctl"), ]]
  text = require("ui.widgets.text"),
}
