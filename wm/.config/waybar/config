{
  "layer": "top",
  "height": 30,
  "mode": "dock",

  "modules-left": [
    "clock"
  ],
  "modules-center": [
    "river/tags"
  ],
  "modules-right": [
    "river/mode",
    "idle_inhibitor",
    "backlight",
    "pulseaudio",
    "bluetooth",
    "network",
    "tray",
    "battery"
  ],

  // Modules
  // ~~~~~~~

  "backlight": {
    "format": "{icon}",
    "format-icons": ["󰃚 ", "󰃛 ", "󰃜", "󰃝 ", "󰃞 ", "󰃟 ", "󰃠 "],
    "on-scroll-up": "light -A 5 && light -G | cut -d'.' -f1 > $XDG_RUNTIME_DIR/wob.sock",
    "on-scroll-up": "light -U 5 && light -G | cut -d'.' -f1 > $XDG_RUNTIME_DIR/wob.sock",
    "tooltip": true,
    "tooltip-format": "{percent}%"
  },

  "battery": {
    "interval": 30,
    "states": {
      "good": 95,
      "warning": 30,
      "critical": 15
    },
    "format": "{icon}",
    "format-charging": "󰂄 ",
    "format-plugged": "󰂅 ",
    "format-icons": ["󰁺", "󰁼", "󰁾", "󰂀", "󰁹"],
    "tooltip": true,
    "tooltip-format": "{capacity}%"
  },

  "bluetooth": {
    "format": "󰂯",
    "format-connected": "󰂱 ",
    "format-disabled": "󰂲",
    "on-click": "blueman-manager",
    "tooltip": true,
    "tooltip-format": "{status}\n{controller_alias}\t{controller_address}",
    "tooltip-format-connected": "{status}\n{controller_alias}\t{controller_address}\n\n{device_enumerate}",
    "tooltip-format-enumerate-connected": "{device_alias}\t{device_address}"
  },

  "clock": {
    "interval": 60,
    "format": "{:%a %b %e %H:%M}",
    "tooltip": false
  },

  "idle_inhibitor": {
    "format": "{icon}",
    "format-icons": {
      "activated": " ",
      "deactivated": " "
    },
    "tooltip": false
  },

  "network": {
    "format-wifi": "󰖩 ",
    "format-ethernet": "󰈀 ",
    "format-disconnected": "󰈂 ",
    "interval": 5,
    "on-click": "nm-connection-editor",
    "tooltip": true,
    "tooltip-format": "{ifname}"
  },

  "pulseaudio": {
    "scroll-step": 5,
    "format": "{icon}",
    "format-bluetooth": "󰥰 ",
    "format-muted": "󰝟 ",
    "format-source": "󰍬",
    "format-source-muted": "󰍭 ",
    "format-icons": {
      "headphone": "󰋋 ",
      "hands-free": "󰹆 ",
      "headset": "󰋎 ",
      "phone": "󰏲 ",
      "portable": "󰏲 ",
      "car": "󰄋 ",
      "default": ["󰕿", "󰖀", "󰕾 "]
    },
    "on-click": "pavucontrol",
    "on-click-middle": "pactl set-sink-mute @DEFAULT_SINK@ toggle && if [ \"$(pactl get-sink-mute @DEFAULT_SINK@ | awk '{print $2}')\" == \"yes\" ]; then echo 0 > $XDG_RUNTIME_DIR/wob.sock; else pactl get-sink-volume @DEFAULT_SINK@ | head -n 1 | awk '{print substr($5, 1, length($5)-1)}' > $XDG_RUNTIME_DIR/wob.sock; fi",
    "on-scroll-up": "pactl set-sink-volume @DEFAULT_SINK@ +5% && if [ \"$(pactl get-sink-mute @DEFAULT_SINK@ | awk '{print $2}')\" == \"yes\" ]; then echo 0 > $XDG_RUNTIME_DIR/wob.sock; else pactl get-sink-volume @DEFAULT_SINK@ | head -n 1 | awk '{print substr($5, 1, length($5)-1)}' > $XDG_RUNTIME_DIR/wob.sock; fi",
    "on-scroll-down": "pactl set-sink-volume @DEFAULT_SINK@ -5% && if [ \"$(pactl get-sink-mute @DEFAULT_SINK@ | awk '{print $2}')\" == \"yes\" ]; then echo 0 > $XDG_RUNTIME_DIR/wob.sock; else pactl get-sink-volume @DEFAULT_SINK@ | head -n 1 | awk '{print substr($5, 1, length($5)-1)}' > $XDG_RUNTIME_DIR/wob.sock; fi",
    "tooltip": true,
    "tooltip-format": "{icon} {volume}% {desc} {format_source}"
  },

  "river/mode": {
    "format": "<span style=\"italic\">{}</span>",
    "tooltip": false
  },

  "river/tags": {
    "num-tags": 6,
    "tag-labels": ["", "", "", "", "", ""],
    "tooltip": false
  },

  "tray": {
    "icon-size": 21,
    "spacing": 5,
    "tooltip": false
  }
}

// vim: ft=jsonc:
